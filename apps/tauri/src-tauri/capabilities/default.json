{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local authority for the My Fin main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
